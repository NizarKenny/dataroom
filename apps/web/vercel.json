{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    {
      "//": "The router lives in the browser, so every path has to be served the app.",
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
